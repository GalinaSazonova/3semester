sdfRenderer.h 
1) Несколько классов объявлено в одном .h;
2) у некоторых public функций отсутствуют комментарии;
3) в 33, 129 строках лишний пробел;
4) 51 строка. Скобочку на другую строку перевести;

 sdfRenderer.cpp 
1) 66 строка. Скобочку на другую строку перевести; 
2) после ключевых слов не стоят пробелы (84, 87  строки и т. д. );
3) многочисленные блоки if-else можно заменить на switch;
4) после условий идет фигурная скобочка — ее на другую строку (например, блок со 146 строчки);
5) где-то после if-ов есть пустые строки,  где-то нет;
6) есть странные сокращения переменных, например, rect, elem, coord (меня за такие ругают), и еще parsestyle -> parseStyle ну и там еще есть такие ребята;
7) 761 строка. Функция которая просто переменную делает false?
8) 880 строка. Странный комментарий ни к чему;
9) 897 строка. Функция просто nullptr возвращает.